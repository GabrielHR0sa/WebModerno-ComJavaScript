const nome = 'Rebeca';
const concatenacao = 'Olá' + nome + '!';


// espressoes
console.log(`1 + 1 = ${1 + 1}`);        //usa-se `` para delimitar a template string e não aspas simples ou duplas

const up = texto => texto.toUpperCase()
console.log(`Ei ... ${up("cuidado")}!`);